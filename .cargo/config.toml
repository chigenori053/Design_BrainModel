[alias]
xtest = "run -p xtask -- test"
test-fast = "run -p xtask -- test fast"
test-architecture = "run -p xtask -- test architecture"
test-invariants = "run -p xtask -- test invariants"
test-engine = "run -p xtask -- test engine"
test-knowledge = "run -p xtask -- test knowledge-engine"
test-contract = "run -p xtask -- test contract"
test-determinism = "run -p xtask -- test determinism"
test-integration = "run -p xtask -- test integration"
test-runtime-heavy = "run -p xtask -- test runtime-heavy"
test-stress = "run -p xtask -- test stress"
test-experiments = "run -p xtask -- test experiments"
test-all-split = "run -p xtask -- test all"
